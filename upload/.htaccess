# Uploaded artwork must never be served directly — only through the
# module's authenticated download action, which checks request ownership.
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order deny,allow
    Deny from all
</IfModule>
